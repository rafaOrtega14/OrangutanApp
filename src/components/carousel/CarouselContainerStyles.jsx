import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  card: {
    height: '100%',
    width: '100%',
    borderRadius: 15,
    borderColor: '#000',
    borderWidth: 5
  }
})
